{

    "routes": [{
            "source": "^/srv/(.*)$",
            "authenticationType": "xsuaa",
            "scope": "$XSAPPNAME.User",
            "destination": "backend-api-provider"
        },
        {
            "source": "^/(.*)",
            "authenticationType": "none",
            "localDir": "resources"
        }
    ]
}